VITE_KEYCLOAK_URL=http://localhost:8181
+ VITE_KEYCLOAK_REALM=chat-app  // Added this line which was previously omitted in the snippet provided by user
VITE_API_BASE_URL=http://localhost:9000

// Note that if you don't have a value for VITE_KEYCLOAK_CLIENT_ID, it will default to 'vite-frontend' as per context/AuthContext.jsx.
